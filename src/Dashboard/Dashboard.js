import React from 'react';
import SideNavLayout from '../Layout/SideNavLayout';

const Dashboard = () => {
    return (
        <div>
            Welcome to our Website.
        </div>
    );
}

export default SideNavLayout(Dashboard);